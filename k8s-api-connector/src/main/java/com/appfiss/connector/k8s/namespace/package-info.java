/**
 * Namespaces are a logical partitioning capability that enable one Kubernetes
 * cluster to be used by multiple users, teams of users, or a single user with
 * multiple applications without concern for undesired interaction. Each user,
 * team of users, or application may exist within its Namespace, isolated from
 * every other user of the cluster and operating as if it were the sole user of
 * the cluster. ,
 */
package com.appfiss.connector.k8s.namespace;